# Base configuration for SkyNet

:defaults: &defaults
  :repository: '<%= repo %>'
  :build_on_startup: true
  :builder: '<%= @builder %>'
  :builder_options:
    :source: '<%= @source %>'
    :destination: 'public'

:development:
  <<: *defaults

:production:
  <<: *defaults
